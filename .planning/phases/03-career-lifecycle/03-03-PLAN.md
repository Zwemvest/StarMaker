---
phase: 03-career-lifecycle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/careers/agent.json
  - src/data/careers/army.json
  - src/data/careers/citizen.json
  - src/data/careers/drifter.json
  - src/data/careers/entertainer.json
  - src/data/careers/marine.json
  - src/data/careers/merchant.json
  - src/data/careers/navy.json
  - src/data/careers/noble.json
  - src/data/careers/rogue.json
  - src/data/careers/scholar.json
  - src/data/careers/scout.json
  - src/data/careers/index.ts
  - tests/data/careers.test.ts
autonomous: true
requirements:
  - CRER-01
  - CRER-02
  - CRER-08
  - CRER-16

must_haves:
  truths:
    - "All 12 career JSON files exist and parse successfully with Zod careerSchema"
    - "Each career has exactly 3 assignments with 6 specialist skills each"
    - "Each career has 11 event entries (rolls 2-12) and 6 mishap entries (rolls 1-6)"
    - "Each career has 7 cash values and 7 benefit entries for mustering out"
    - "Military careers (Army, Marines, Navy) have commission targets and officer ranks/tables"
    - "Citizen and Drifter have basicTrainingException set to true"
  artifacts:
    - path: "src/data/careers/agent.json"
      provides: "Agent career data"
      contains: "Agent"
    - path: "src/data/careers/index.ts"
      provides: "Career data index with Zod validation"
      exports: ["CAREERS", "getCareer"]
    - path: "tests/data/careers.test.ts"
      provides: "Structural validation tests for all 12 careers"
      min_lines: 50
  key_links:
    - from: "src/data/careers/index.ts"
      to: "src/schemas/career.ts"
      via: "Zod validation on import"
      pattern: "careerSchema.parse"
    - from: "src/data/careers/index.ts"
      to: "src/data/careers/*.json"
      via: "static JSON imports"
      pattern: "import.*from.*\\.json"
---

<objective>
Transcribe all 12 career data files from the Core Rulebook PDF into JSON format and create the data index with Zod validation.

Purpose: This is the single largest data transcription task in the project (~1,200 entries). Each career needs qualification targets, 3 assignments with survival/advancement targets and specialist skills, rank tables with bonus skills, 5 skill training tables, 11 event entries, 6 mishap entries, and mustering out tables. Per D-08, these are JSON files validated by Zod schemas.
Output: 12 career JSON files, index.ts with validated imports, structural tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-career-lifecycle/03-CONTEXT.md
@.planning/phases/03-career-lifecycle/03-RESEARCH.md
@.planning/phases/03-career-lifecycle/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and schemas this plan depends on -->

From src/types/careers.ts (extended by Plan 01):
```typescript
export type CareerName = 'agent' | 'army' | 'citizen' | 'drifter' | 'entertainer' | 'marine' | 'merchant' | 'navy' | 'noble' | 'rogue' | 'scholar' | 'scout';
export interface CareerData { /* full career structure */ }
export type SkillEntry = string | { name: string; specialty?: string };
```

From src/schemas/career.ts (created by Plan 01):
```typescript
export const careerSchema: z.ZodType<CareerData>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Transcribe first 6 career JSON files (Agent through Marine)</name>
  <files>src/data/careers/agent.json, src/data/careers/army.json, src/data/careers/citizen.json, src/data/careers/drifter.json, src/data/careers/entertainer.json, src/data/careers/marine.json</files>
  <read_first>"MgT 2E - Core Rulebook (Printer Friendly).pdf", src/types/careers.ts, src/schemas/career.ts</read_first>
  <action>
    Read the Core Rulebook PDF career sections. For EACH of the 6 careers below, create a JSON file in src/data/careers/ with:

    1. **name**: Career name (capitalized)
    2. **description**: Brief career description from rulebook
    3. **qualification**: { characteristic: "XXX", target: N } or null for Drifter (auto-entry)
    4. **isMilitary**: true for Army, Marines; false for others
    5. **commission**: { characteristic: "SOC", target: N } for military careers; null for civilians
    6. **basicTrainingException**: true for Citizen and Drifter ONLY; false for all others
    7. **assignments** (array of 3): Each with:
       - name, description
       - survival: { characteristic, target }
       - advancement: { characteristic, target }
       - specialistSkills: array of exactly 6 SkillEntry values
    8. **ranks**: { enlisted: array of RankEntry (7 entries, levels 0-6), officer: array of RankEntry (7 entries, levels 0-6) for military only }
       - Each rank: { level, title, bonusSkill (or null), bonusSkillLevel (0 or 1, optional) }
    9. **skillTables**: { personalDevelopment: 6 entries, serviceSkills: 6 entries, advancedEducation: 6 entries, officer: 6 entries (military only) }
    10. **events**: 11 entries for 2D table (rolls 2-12). Each with rollValue, description (narrative text), effectDescription (mechanical text), effects array, hasChoice boolean
    11. **mishaps**: 6 entries for 1D table (rolls 1-6). Same structure as events.
    12. **musteringOut**: { cash: [7 numbers], benefits: [7 strings] }

    Careers in this batch:
    - Agent (Law Enforcement, Intelligence, Corporate)
    - Army (Support, Infantry, Cavalry) — MILITARY: has commission and officer ranks/tables
    - Citizen (Corporate, Worker, Colonist) — basicTrainingException: true
    - Drifter (Barbarian, Wanderer, Scavenger) — qualification: null, basicTrainingException: true
    - Entertainer (Artist, Journalist, Performer)
    - Marine (Support, Star Marine, Ground Assault) — MILITARY: has commission and officer ranks/tables

    CRITICAL: Read every value directly from the PDF. Do not guess or approximate. Pay special attention to:
    - Correct characteristic abbreviations (STR, DEX, END, INT, EDU, SOC)
    - Exact target numbers for qualification, survival, advancement, commission
    - Exact skill names matching existing skill naming conventions
    - Correct rank titles at each level
    - Whether bonusSkill is null or has a specific skill at a specific level
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const careers=['agent','army','citizen','drifter','entertainer','marine']; careers.forEach(c => { const d=JSON.parse(fs.readFileSync('src/data/careers/'+c+'.json','utf8')); console.log(c+': assignments='+d.assignments.length+' events='+d.events.length+' mishaps='+d.mishaps.length); })"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 JSON files exist in src/data/careers/
    - Each file has exactly 3 assignments, 11 events, 6 mishaps
    - Citizen and Drifter JSON have "basicTrainingException": true
    - Army, Marine JSON have "isMilitary": true and non-null commission
    - Drifter JSON has "qualification": null
  </acceptance_criteria>
  <done>First 6 career JSON files faithfully transcribed from Core Rulebook.</done>
</task>

<task type="auto">
  <name>Task 1b: Transcribe remaining 6 career JSON files (Merchant through Scout)</name>
  <files>src/data/careers/merchant.json, src/data/careers/navy.json, src/data/careers/noble.json, src/data/careers/rogue.json, src/data/careers/scholar.json, src/data/careers/scout.json</files>
  <read_first>"MgT 2E - Core Rulebook (Printer Friendly).pdf", src/types/careers.ts, src/schemas/career.ts</read_first>
  <action>
    Continue transcribing career JSON files from the Core Rulebook PDF. Same JSON structure as Task 1a.

    Careers in this batch:
    - Merchant (Merchant Marine, Free Trader, Broker)
    - Navy (Line/Crew, Engineer/Gunner, Flight) — MILITARY: has commission and officer ranks/tables
    - Noble (Administrator, Diplomat, Dilettante)
    - Rogue (Thief, Enforcer, Pirate)
    - Scholar (Field Researcher, Scientist, Physician)
    - Scout (Courier, Survey, Exploration)

    Same CRITICAL rules apply: read every value directly from the PDF, no guessing.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const careers=['merchant','navy','noble','rogue','scholar','scout']; careers.forEach(c => { const d=JSON.parse(fs.readFileSync('src/data/careers/'+c+'.json','utf8')); console.log(c+': assignments='+d.assignments.length+' events='+d.events.length+' mishaps='+d.mishaps.length); })"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 JSON files exist in src/data/careers/
    - Each file has exactly 3 assignments, 11 events, 6 mishaps
    - Navy JSON has "isMilitary": true and non-null commission
    - All others have "isMilitary": false
  </acceptance_criteria>
  <done>Remaining 6 career JSON files faithfully transcribed from Core Rulebook.</done>
</task>

<task type="auto">
  <name>Task 2: Create career data index and structural tests</name>
  <files>src/data/careers/index.ts, tests/data/careers.test.ts</files>
  <read_first>src/schemas/career.ts, src/types/careers.ts, src/data/careers/agent.json</read_first>
  <action>
    Create src/data/careers/index.ts:
    - Import all 12 JSON files statically (for Vite bundling)
    - Import careerSchema from schemas/career.ts
    - Import CareerName, CareerData from types/careers.ts
    - Parse each JSON through careerSchema.parse() on module load (per D-08: Zod runtime validation)
    - Export CAREERS as Record<CareerName, CareerData>
    - Export getCareer(name: CareerName): CareerData helper function
    - Export ALL_CAREER_NAMES as readonly CareerName[] for iteration

    Create tests/data/careers.test.ts with structural tests (per D-09):
    - Test that all 12 career names are present in CAREERS
    - Test each career has exactly 3 assignments
    - Test each assignment has exactly 6 specialist skills
    - Test each career has exactly 11 events (rolls 2-12) with sequential rollValues
    - Test each career has exactly 6 mishaps (rolls 1-6) with sequential rollValues
    - Test military careers (army, marine, navy) have isMilitary=true and non-null commission
    - Test civilian careers have isMilitary=false and null commission
    - Test citizen and drifter have basicTrainingException=true
    - Test drifter has qualification=null
    - Test each career has 7 cash values and 7 benefit entries
    - Test all rank entries have valid structure
    - Test all skill tables have exactly 6 entries each
  </action>
  <verify>
    <automated>npx vitest run tests/data/careers.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CAREERS" src/data/careers/index.ts returns at least 1
    - grep -c "getCareer" src/data/careers/index.ts returns at least 1
    - grep -c "careerSchema" src/data/careers/index.ts returns at least 1
    - grep -c "describe" tests/data/careers.test.ts returns at least 3
    - npx vitest run tests/data/careers.test.ts exits with code 0
  </acceptance_criteria>
  <done>Career index validates all 12 JSON files through Zod on import. Structural tests verify correct data shapes for all careers.</done>
</task>

</tasks>

<verification>
- npx vitest run tests/data/careers.test.ts --reporter=verbose — all structural tests pass
- All 12 JSON files parse through Zod without errors (tested via index.ts import)
- Military careers have commission and officer tables; civilians don't
</verification>

<success_criteria>
- All 12 career JSON files exist with complete data transcribed from Core Rulebook
- Every JSON file validates through careerSchema.parse() without errors
- Structural tests confirm correct array lengths and field presence
- Career index exports CAREERS map and getCareer helper
- Data is faithful to the Core Rulebook (qualification targets, skill names, rank titles, event descriptions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-career-lifecycle/03-03-SUMMARY.md`
</output>
