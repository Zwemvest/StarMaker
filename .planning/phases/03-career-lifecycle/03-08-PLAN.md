---
phase: 03-career-lifecycle
plan: 08
type: execute
wave: 5
depends_on: [03-07]
files_modified:
  - src/components/character-panel/CharacterPanel.tsx
  - src/engine/format-roll-context.ts
  - tests/engine/golden-path.test.ts
autonomous: false
requirements:
  - SOCL-01
  - SOCL-02
  - CRER-01

must_haves:
  truths:
    - "Character panel shows career history with terms, ranks, and events"
    - "Contacts/Allies/Rivals/Enemies section shows counts and expandable details"
    - "Noble titles appear next to SOC value when SOC >= 11"
    - "Golden-path tests verify end-to-end career data accuracy"
    - "Complete career lifecycle can be navigated from career grid to mustering out"
  artifacts:
    - path: "src/components/character-panel/CharacterPanel.tsx"
      provides: "Extended panel with career history and contacts per D-13, D-17, D-18"
      contains: "careerHistory"
    - path: "tests/engine/golden-path.test.ts"
      provides: "Golden-path integration tests per D-09"
      min_lines: 50
  key_links:
    - from: "src/components/character-panel/CharacterPanel.tsx"
      to: "src/stores/character.ts"
      via: "career data from store"
      pattern: "useCharacterStore"
    - from: "src/components/character-panel/CharacterPanel.tsx"
      to: "src/engine/career.ts"
      via: "noble title lookup"
      pattern: "getNobleTitle"
---

<objective>
Extend the character panel with career history, contacts, and noble titles. Create golden-path integration tests. Human verification of the complete career lifecycle.

Purpose: The character panel (D-13) is always visible in the right sidebar and must update live as careers progress. Golden-path tests (D-09) verify that the career data and engine produce correct results for pre-rolled characters. This plan also includes the final human verification checkpoint.
Output: Extended CharacterPanel, golden-path tests, verified career lifecycle.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-career-lifecycle/03-CONTEXT.md
@.planning/phases/03-career-lifecycle/03-07-SUMMARY.md

<interfaces>
From src/stores/character.ts (extended by Plan 04):
```typescript
// Career fields:
careerHistory: CareerTerm[];
contacts: Contact[];
age: number;
credits: number;
pension: number;
benefits: string[];
```

From src/engine/career.ts (Plan 01):
```typescript
export function getNobleTitle(soc: number): string | null;
```

From src/types/character.ts:
```typescript
export interface Contact { type: 'contact' | 'ally' | 'rival' | 'enemy'; name: string; notes: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CharacterPanel with career history, contacts, and noble titles</name>
  <files>src/components/character-panel/CharacterPanel.tsx, src/engine/format-roll-context.ts</files>
  <read_first>src/components/character-panel/CharacterPanel.tsx, src/stores/character.ts, src/engine/career.ts, src/engine/format-roll-context.ts</read_first>
  <action>
    Extend CharacterPanel.tsx to show career data (per D-13, D-17, D-18):

    1. **Career History section** (D-13):
       - Collapsible section titled "Career History"
       - Shows each career term: "{Career Name} - {Assignment} (Term {N})"
       - Shows rank achieved and key event summaries
       - Updates live as careers progress
       - Default expanded during career step, collapsible after

    2. **Contacts section** (D-17):
       - Collapsible section titled "Contacts"
       - Shows grouped counts: "Allies: 2, Contacts: 3, Rivals: 1, Enemies: 0"
       - Expandable details showing each contact with notes
       - Brief notification integration point (events that create contacts)

    3. **Noble Titles** (D-18):
       - When SOC >= 11, show noble title next to SOC value
       - Use getNobleTitle(soc) from engine/career.ts
       - Display as: "SOC 12 (Baron)" in the characteristics display
       - Updates live when SOC changes (e.g., from events or aging)
       - Titles: 11=Knight, 12=Baron, 13=Marquis, 14=Count, 15+=Duke (SOCL-02)

    4. **Financial summary** (visible during/after mustering out):
       - Credits total
       - Benefits list
       - Pension (if applicable)

    Update src/engine/format-roll-context.ts:
    - Add career roll context format strings:
      - "career-survival" -> "Career Survival Roll"
      - "career-commission" -> "Commission Roll"
      - "career-advancement" -> "Advancement Roll"
      - "career-event" -> "Career Event Roll"
      - "career-skill" -> "Career Skill Roll"
      - "aging-check" -> "Aging Check ({stat})"
      - "mustering-cash" -> "Mustering Out Cash Roll"
      - "mustering-benefit" -> "Mustering Out Benefits Roll"
      - "draft" -> "Draft Roll"
      - "qualification" -> "Career Qualification Roll"
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "careerHistory" src/components/character-panel/CharacterPanel.tsx returns at least 1
    - grep -c "contacts" src/components/character-panel/CharacterPanel.tsx returns at least 1
    - grep -c "getNobleTitle" src/components/character-panel/CharacterPanel.tsx returns at least 1
    - grep -c "pension" src/components/character-panel/CharacterPanel.tsx returns at least 1
    - grep -c "career-survival" src/engine/format-roll-context.ts returns at least 1
  </acceptance_criteria>
  <done>Character panel shows career history, contacts with counts and details, noble titles, and financial summary. Roll context labels added for all career roll types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create golden-path integration tests</name>
  <files>tests/engine/golden-path.test.ts</files>
  <read_first>src/data/careers/index.ts, src/engine/career.ts, src/engine/aging.ts, src/engine/mustering-out.ts, src/data/careers/army.json, src/data/careers/scout.json, "MgT 2E - Core Rulebook (Printer Friendly).pdf"</read_first>
  <behavior>
    - Golden path 1: Army Infantry character with 3 terms. Verify: correct qualification target (END 5+), correct Infantry survival (STR 6+) and advancement (EDU 6+), correct rank titles (Private, Lance Corporal, Corporal...), correct service skills, correct mustering out values.
    - Golden path 2: Scout Exploration character with 2 terms. Verify: correct qualification (INT 5+), correct survival (END 7+), no commission (not military), correct event table values, mustering out cash/benefit values.
    - Golden path 3: Merchant Free Trader with 1 term ending in mishap. Verify: correct mishap table lookup, lost benefit handling, no pension (< 5 terms).
    - Golden path 4: Noble Diplomat with 5+ terms. Verify: pension qualification, aging at 34+, noble title from SOC.
    - Golden path 5: Drifter with no qualification required. Verify: auto-entry, basicTrainingException uses assignment skills.
  </behavior>
  <action>
    Create tests/engine/golden-path.test.ts (per D-09):

    These tests simulate pre-rolled characters by calling engine functions with specific dice values and verifying the results match the Core Rulebook exactly. They do NOT test UI — they test data + engine accuracy.

    For each golden path character:
    1. Load the career data via getCareer()
    2. Verify qualification target matches rulebook
    3. Call resolveQualificationRoll with specific dice, verify result
    4. Verify correct assignment survival/advancement targets
    5. Call resolveSurvivalRoll with specific dice, verify result
    6. Verify event table entry at specific roll value matches expected description
    7. Call resolveAdvancementRoll with specific dice, verify result
    8. Verify rank table entry at specific level has correct title and bonus skill
    9. Verify mustering out cash table has correct values
    10. For aging paths: call getAgingChecks at correct age, verify targets
    11. For pension paths: call calculatePension, verify amount
    12. For mishap paths: verify mishap entry at specific roll has correct description

    **Pre-rolled characters to test (all values from Core Rulebook):**

    GP1 - Army Infantry:
    - Qualification: END 5+ (verify career.qualification = { characteristic: "END", target: 5 })
    - Infantry survival: STR 6+ (verify assignments[1].survival = { characteristic: "STR", target: 6 })
    - Infantry advancement: EDU 6+ (verify assignments[1].advancement)
    - Rank 0: "Private" (verify ranks.enlisted[0].title)
    - Rank 1: "Lance Corporal", bonus: "Gun Combat 1" (verify ranks.enlisted[1])

    GP2 - Scout Exploration:
    - Qualification: INT 5+ (verify)
    - Not military (verify isMilitary === false)
    - Exploration survival: END 7+ (verify)
    - No commission (verify commission === null)

    GP3 - Merchant Free Trader:
    - Qualification: INT 4+ (verify)
    - Mishap roll 1: verify mishap description matches rulebook

    GP4 - Noble Diplomat:
    - Qualification: SOC 10+ (verify)
    - Pension at 5 terms: Cr10,000 (verify calculatePension(5))
    - Aging at 34: STR 8, DEX 7, END 8 (verify getAgingChecks(34))

    GP5 - Drifter:
    - qualification === null (auto-entry, verify)
    - basicTrainingException === true (verify)
    - Barbarian specialist skills are used for basic training (verify)

    Read the Core Rulebook PDF for exact values to use in assertions. Every assertion must match the book exactly.
  </action>
  <verify>
    <automated>npx vitest run tests/engine/golden-path.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "golden" tests/engine/golden-path.test.ts returns at least 1
    - grep -c "describe" tests/engine/golden-path.test.ts returns at least 5
    - grep -c "qualification" tests/engine/golden-path.test.ts returns at least 5
    - npx vitest run tests/engine/golden-path.test.ts exits with code 0
  </acceptance_criteria>
  <done>5 golden-path characters verify end-to-end career data accuracy against Core Rulebook. All pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete career lifecycle</name>
  <files>none</files>
  <action>
    Verify the complete career lifecycle works end-to-end in the browser.

    What was built: Complete career lifecycle including career selection grid, assignment cards, qualification rolls, basic training, term loop (survival, events, commission, advancement, skill selection), aging, continue/leave decisions, multi-career transitions, mustering out with benefits/pension, character panel with career history/contacts/noble titles, and vertical timeline.

    Steps to verify:
    1. Run `npm run dev` and open in browser
    2. Create a new character through characteristics and background skills
    3. Skip or complete education
    4. Verify career grid shows 12 careers with qualification info
    5. Select Army career, choose Infantry assignment
    6. Verify qualification roll with correct target
    7. Complete a full term: survival roll, event, commission (military), advancement, skill selection
    8. Verify aging warning appears at 34+ (serve enough terms)
    9. Verify continue/leave card shows pension info
    10. Muster out and verify roll-by-roll benefits with cash limit
    11. Check character panel: career history, contacts, noble title (if SOC >= 11)
    12. Try a second character with Drifter (no qualification, assignment basic training)
    13. Try qualification failure -> Draft or Drifter choice
  </action>
  <verify>User confirms career lifecycle works correctly in browser</verify>
  <done>User has approved the complete career lifecycle or provided issues to fix. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- npx vitest run — full test suite passes including golden-path tests
- npm run build — no TypeScript errors
- Character panel shows career data live
- Golden-path tests verify data accuracy against Core Rulebook
</verification>

<success_criteria>
- Character panel shows career history, contacts, and noble titles live (D-13, D-17, D-18, SOCL-01, SOCL-02)
- Golden-path tests verify 5 pre-rolled characters match Core Rulebook exactly (D-09)
- Complete career lifecycle navigable from grid to mustering out
- All 34 phase requirements addressed across all plans
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-career-lifecycle/03-08-SUMMARY.md`
</output>
