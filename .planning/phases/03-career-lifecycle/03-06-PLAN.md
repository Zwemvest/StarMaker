---
phase: 03-career-lifecycle
plan: 06
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-04]
files_modified:
  - src/components/career/SurvivalRoll.tsx
  - src/components/career/CareerEventCard.tsx
  - src/components/career/CommissionCard.tsx
  - src/components/career/AdvancementCard.tsx
  - src/components/career/SkillTableTabs.tsx
  - src/components/career/MishapCard.tsx
autonomous: true
requirements:
  - CRER-06
  - CRER-07
  - CRER-08
  - CRER-09
  - CRER-10
  - CRER-11
  - CRER-12
  - CRER-13
  - CRER-14
  - CRER-15
  - CRER-16
  - CRER-17
  - CRER-18
  - CRER-19

must_haves:
  truths:
    - "Dramatic survival roll shows target, DM, and reveals pass/fail with visual effects"
    - "Career events display narrative text with mechanical effects and choices"
    - "Commission and advancement rolls work correctly for military careers"
    - "Tabbed skill selection shows available tables based on rank and EDU"
    - "Skill level 4 cap is enforced in skill selection UI"
    - "Mishap card displays mishap result when survival fails"
  artifacts:
    - path: "src/components/career/SurvivalRoll.tsx"
      provides: "Dramatic survival roll card per D-05"
      contains: "SurvivalRoll"
    - path: "src/components/career/CareerEventCard.tsx"
      provides: "Career event display per D-03"
      contains: "CareerEventCard"
    - path: "src/components/career/CommissionCard.tsx"
      provides: "Commission roll card per D-06"
      contains: "CommissionCard"
    - path: "src/components/career/AdvancementCard.tsx"
      provides: "Advancement roll card per D-06"
      contains: "AdvancementCard"
    - path: "src/components/career/SkillTableTabs.tsx"
      provides: "Tabbed skill table picker per D-04"
      contains: "SkillTableTabs"
    - path: "src/components/career/MishapCard.tsx"
      provides: "Mishap display card"
      contains: "MishapCard"
  key_links:
    - from: "src/components/career/SurvivalRoll.tsx"
      to: "src/engine/career.ts"
      via: "resolveSurvivalRoll"
      pattern: "resolveSurvivalRoll"
    - from: "src/components/career/SkillTableTabs.tsx"
      to: "src/engine/career.ts"
      via: "getAvailableSkillTables, isSkillAtCap"
      pattern: "getAvailableSkillTables|isSkillAtCap"
    - from: "src/components/career/AdvancementCard.tsx"
      to: "src/engine/career.ts"
      via: "resolveAdvancementRoll"
      pattern: "resolveAdvancementRoll"
---

<objective>
Build all career term loop UI components: survival roll, events, commission, advancement, skill selection, and mishap display.

Purpose: These are the core term cycle components that the user interacts with each career term. Each follows the step-by-step card pattern (D-01) with dramatic reveals for rolls (D-05) and narrative text for events (D-03).
Output: SurvivalRoll, CareerEventCard, CommissionCard, AdvancementCard, SkillTableTabs, MishapCard components.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-career-lifecycle/03-CONTEXT.md
@.planning/phases/03-career-lifecycle/03-01-SUMMARY.md
@.planning/phases/03-career-lifecycle/03-04-SUMMARY.md

<interfaces>
From src/engine/career.ts (Plan 01):
```typescript
export function resolveSurvivalRoll(diceTotal, dm, target): { survived, isMishap, naturalTwo };
export function resolveCommissionRoll(diceTotal, dm, target, termsInCareer): { success, total };
export function resolveAdvancementRoll(diceTotal, dm, target, termsServed): { advanced, forcedToLeave, forcedToStay };
export function getAvailableSkillTables(career, isCommissioned, edu): string[];
export function applyRankSkill(career, newRank, isOfficer): { skill, level } | null;
export function isSkillAtCap(currentLevel): boolean;
export function isOverSkillLimit(skills, int, edu): boolean;
```

From src/hooks/useLoggedRoll.ts:
```typescript
export function useLoggedRoll(): { roll2D, roll1D };
```

From src/components/education/EventCard.tsx:
```typescript
// Narrative event card pattern — scanner-blue border, italic flavor text
```

From src/machines/creation.ts (Plan 04):
```typescript
// Term loop events:
| { type: 'SURVIVAL_SUCCESS' }
| { type: 'SURVIVAL_FAILURE' }
| { type: 'MISHAP_RESOLVED' }
| { type: 'EVENT_RESOLVED' }
| { type: 'COMMISSION_RESULT'; success: boolean }
| { type: 'ADVANCEMENT_RESULT'; advanced: boolean; forcedToLeave: boolean; forcedToStay: boolean }
| { type: 'SKILL_SELECTED' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SurvivalRoll, MishapCard, and CareerEventCard</name>
  <files>src/components/career/SurvivalRoll.tsx, src/components/career/MishapCard.tsx, src/components/career/CareerEventCard.tsx</files>
  <read_first>src/components/education/EventCard.tsx, src/components/education/EducationStep.tsx, src/engine/career.ts, src/hooks/useLoggedRoll.ts, src/data/life-events.ts, src/types/careers.ts</read_first>
  <action>
    Create src/components/career/SurvivalRoll.tsx (per D-05):
    - Props: assignment: AssignmentData, characteristicValue: number, onSurvived: () => void, onMishap: (rollValue: number) => void
    - Display: survival target ("{characteristic} {target}+"), calculated DM from characteristicModifier, current DM value
    - "Roll for Survival" button — dramatic, prominent, high-stakes styling
    - Uses useLoggedRoll().roll2D with context "Career Survival Roll"
    - Calls resolveSurvivalRoll(diceTotal, dm, target)
    - On success: green relief glow, brief pause, then calls onSurvived
    - On failure: red flash effect, shows "MISHAP!" text, then calls onMishap with 1D roll for mishap table
    - Natural 2: extra dramatic "Natural 2 — Automatic Failure!" text regardless of DM (CRER-07)

    Create src/components/career/MishapCard.tsx:
    - Props: mishap: MishapEntry, onResolved: () => void
    - Displays mishap description in red-bordered card
    - Shows effectDescription with mechanical effects
    - Applies effects to store (skill changes, injuries, contacts/rivals/enemies per SOCL-01)
    - "Continue" button dispatches MISHAP_RESOLVED (exits career to mustering out)
    - Note: mishap means forced exit from career

    Create src/components/career/CareerEventCard.tsx (per D-03):
    - Props: event: CareerEventEntry, lifeEvent?: LifeEvent (for roll=7 redirect to life events, CRER-09), onResolved: () => void
    - Same scanner-blue border, italic flavor text pattern as education EventCard
    - Shows event description (narrative text) and effectDescription (mechanical)
    - If event.hasChoice: display choice buttons for the options
    - Applies effects to store: skill grants via addSkill, contact creation via addContact (SOCL-01), characteristic changes
    - For event roll=7: redirect to life events table (CRER-09). Roll on LIFE_EVENTS, display that instead.
    - "Continue" button dispatches EVENT_RESOLVED
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SurvivalRoll" src/components/career/SurvivalRoll.tsx returns at least 1
    - grep -c "resolveSurvivalRoll" src/components/career/SurvivalRoll.tsx returns at least 1
    - grep -c "naturalTwo" src/components/career/SurvivalRoll.tsx returns at least 1
    - grep -c "MishapCard" src/components/career/MishapCard.tsx returns at least 1
    - grep -c "CareerEventCard" src/components/career/CareerEventCard.tsx returns at least 1
    - grep -c "LIFE_EVENTS" src/components/career/CareerEventCard.tsx returns at least 1
  </acceptance_criteria>
  <done>Survival roll shows dramatic pass/fail with natural-2 handling. Mishap card displays mishap effects. Event card follows D-03 narrative pattern with life events redirect for roll 7.</done>
</task>

<task type="auto">
  <name>Task 2: Create CommissionCard, AdvancementCard, and SkillTableTabs</name>
  <files>src/components/career/CommissionCard.tsx, src/components/career/AdvancementCard.tsx, src/components/career/SkillTableTabs.tsx</files>
  <read_first>src/engine/career.ts, src/types/careers.ts, src/hooks/useLoggedRoll.ts, src/stores/character.ts, src/machines/creation.ts, src/components/education/EducationStep.tsx</read_first>
  <action>
    Create src/components/career/CommissionCard.tsx (per D-06):
    - Props: commissionTarget: { characteristic, target }, characteristicValue: number, termsInCareer: number, onResult: (success: boolean) => void
    - Only shown for military careers when not yet commissioned
    - Shows: target number, characteristic DM, term penalty DM (-1 per term after first, CRER-11)
    - "Roll for Commission" button
    - Uses useLoggedRoll().roll2D with context "Commission Roll"
    - Calls resolveCommissionRoll(diceTotal, dm, target, termsInCareer)
    - Success: shows "Commissioned!" with new officer rank. Dispatches COMMISSION_RESULT { success: true }
    - Failure: shows "Not Commissioned" briefly. Dispatches COMMISSION_RESULT { success: false }
    - On new commission: apply rank 1 officer bonus skill via applyRankSkill, add to store

    Create src/components/career/AdvancementCard.tsx (per D-06):
    - Props: advancementTarget: { characteristic, target }, characteristicValue: number, termsServed: number, isOfficer: boolean, onResult: (result) => void
    - Shows: target number, characteristic DM
    - "Roll for Advancement" button
    - Uses useLoggedRoll().roll2D with context "Advancement Roll"
    - Calls resolveAdvancementRoll(diceTotal, dm, target, termsServed)
    - Advanced: shows "Promoted!" with new rank title. Apply rank bonus skill (CRER-15). Increment rank in store.
    - Forced to leave (CRER-13): shows "Your career is stalling..." warning
    - Forced to stay natural 12 (CRER-14): shows "Your career is thriving! Forced to continue."
    - Not advanced: brief "No Promotion" message
    - Dispatches ADVANCEMENT_RESULT with { advanced, forcedToLeave, forcedToStay }

    Create src/components/career/SkillTableTabs.tsx (per D-04):
    - Props: career: CareerData, isCommissioned: boolean, edu: number, skills: Skill[], int: number, onSkillSelected: () => void
    - Uses getAvailableSkillTables(career, isCommissioned, edu) to determine which tabs to show
    - Available tabs: Personal Development, Service Skills, Specialist (of current assignment), Advanced Education (EDU 8+, CRER-17), Officer (commissioned only, CRER-17)
    - Each tab shows 6 skills in a list
    - User rolls 1D using useLoggedRoll().roll1D to select from table, OR can pick directly (Claude's discretion: use roll-to-select for faithfulness to rules)
    - Actually, per the rulebook: player chooses which TABLE, then rolls 1D to determine which SKILL from that table. Implement this: user clicks tab, then clicks "Roll for Skill" button, 1D determines the skill.
    - Show rolled skill result. If skill is at level 4 cap (CRER-18), show warning and grant at current level. If already owned, increment level.
    - Check isOverSkillLimit after granting (CRER-19). Show warning if approaching limit.
    - Call store.addSkill with the result. Dispatch SKILL_SELECTED.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CommissionCard" src/components/career/CommissionCard.tsx returns at least 1
    - grep -c "resolveCommissionRoll" src/components/career/CommissionCard.tsx returns at least 1
    - grep -c "AdvancementCard" src/components/career/AdvancementCard.tsx returns at least 1
    - grep -c "resolveAdvancementRoll" src/components/career/AdvancementCard.tsx returns at least 1
    - grep -c "forcedToStay" src/components/career/AdvancementCard.tsx returns at least 1
    - grep -c "SkillTableTabs" src/components/career/SkillTableTabs.tsx returns at least 1
    - grep -c "getAvailableSkillTables" src/components/career/SkillTableTabs.tsx returns at least 1
    - grep -c "isSkillAtCap" src/components/career/SkillTableTabs.tsx returns at least 1
  </acceptance_criteria>
  <done>Commission and advancement cards implement full military career flow with D-06 sequential pattern. SkillTableTabs shows available tables as tabs with roll-to-select mechanic. Skill caps enforced in UI.</done>
</task>

</tasks>

<verification>
- npx vitest run -- full suite passes (no regressions, confirms TypeScript compilation of all new components)
- Note: UI component rendering is human-verified in Plan 03-08 Task 3
- All 6 term loop components render correctly
- Survival roll handles natural 2 special case
- Commission applies DM-1 per term after first
- Advancement tracks forced leave and forced stay
- Skill selection respects table restrictions and level cap
</verification>

<success_criteria>
- Dramatic survival roll with natural-2 always-fail handling (D-05, CRER-07)
- Career events follow narrative card pattern with life events redirect (D-03, CRER-08, CRER-09)
- Commission rolls apply term DM penalty (CRER-10, CRER-11)
- Advancement detects forced leave and forced stay (CRER-13, CRER-14)
- Rank bonus skills granted immediately on promotion (CRER-15)
- Skill tables show correct tabs based on rank and EDU (CRER-16, CRER-17)
- Skill level 4 cap shown in UI (CRER-18)
- Total skill limit warning shown (CRER-19)
- Mishap card shows effects and forces career exit (CRER-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-career-lifecycle/03-06-SUMMARY.md`
</output>
