---
phase: 03-career-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/aging.ts
  - src/engine/mustering-out.ts
  - src/data/life-events.ts
  - src/data/draft.ts
  - src/data/aging.ts
  - tests/engine/aging.test.ts
  - tests/engine/mustering-out.test.ts
autonomous: true
requirements:
  - AGNG-01
  - AGNG-02
  - AGNG-03
  - MSTR-01
  - MSTR-02
  - MSTR-03
  - MSTR-04
  - MSTR-05
  - CRER-09
  - SOCL-01

must_haves:
  truths:
    - "Aging checks trigger at age 34+ with correct characteristic reduction targets per age bracket"
    - "Aging crisis is detected when a characteristic would reach 0"
    - "Mustering out calculates correct benefit roll count with rank bonuses"
    - "Cash table limited to max 3 rolls lifetime"
    - "Pension calculated correctly for 5+ terms of service"
    - "Mishap term loses one benefit roll"
    - "Life events table has all entries for shared career events"
  artifacts:
    - path: "src/engine/aging.ts"
      provides: "Pure aging engine functions"
      exports: ["getAgingChecks", "resolveAgingCheck", "isAgingCrisis"]
    - path: "src/engine/mustering-out.ts"
      provides: "Pure mustering out engine functions"
      exports: ["calculateBenefitRolls", "calculatePension", "resolveCashRoll", "resolveBenefitRoll"]
    - path: "src/data/life-events.ts"
      provides: "Life events table data"
      exports: ["LIFE_EVENTS"]
    - path: "src/data/aging.ts"
      provides: "Aging table data"
      exports: ["AGING_TABLE"]
    - path: "src/data/draft.ts"
      provides: "Draft table data"
      exports: ["DRAFT_TABLE"]
  key_links:
    - from: "src/engine/aging.ts"
      to: "src/data/aging.ts"
      via: "import aging table"
      pattern: "AGING_TABLE"
    - from: "src/engine/mustering-out.ts"
      to: "src/types/careers.ts"
      via: "type imports"
      pattern: "import.*from.*types/careers"
---

<objective>
Create aging engine, mustering-out engine, life events data, draft table, and aging table data with comprehensive tests.

Purpose: These are the supporting game engines and shared data tables that the career term loop and mustering out UI will consume. Separating them from the career engine keeps each file focused and testable.
Output: aging.ts engine + data, mustering-out.ts engine, life-events.ts, draft.ts, and tests for all.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-career-lifecycle/03-CONTEXT.md
@.planning/phases/03-career-lifecycle/03-RESEARCH.md

<interfaces>
From src/types/common.ts:
```typescript
export type CharacteristicId = 'STR' | 'DEX' | 'END' | 'INT' | 'EDU' | 'SOC';
export function characteristicModifier(value: number): number;
```

From src/types/character.ts:
```typescript
export interface Skill { name: string; level: number; }
export interface Contact { type: 'contact' | 'ally' | 'rival' | 'enemy'; name: string; notes: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create aging data, engine, and tests</name>
  <files>src/data/aging.ts, src/engine/aging.ts, tests/engine/aging.test.ts</files>
  <read_first>src/types/common.ts, src/engine/education.ts, "MgT 2E - Core Rulebook (Printer Friendly).pdf"</read_first>
  <behavior>
    - getAgingChecks(30) returns null (no aging before 34)
    - getAgingChecks(34) returns STR target 8, DEX target 7, END target 8 (age 34-45 bracket)
    - getAgingChecks(46) returns STR target 9, DEX target 8, END target 9 (age 46-57 bracket)
    - getAgingChecks(58) returns STR target 10, DEX target 9, END target 10 (age 58-69 bracket)
    - getAgingChecks(70) returns STR target 11, DEX target 10, END target 11 (age 70+ bracket)
    - getAgingChecks(80) returns same as 70 bracket
    - resolveAgingCheck(8, 0, 8) returns { reduced: false, amount: 0 } (met target)
    - resolveAgingCheck(6, 0, 8) returns { reduced: true, amount: 2 } (missed by 2)
    - isAgingCrisis(2, 3) returns true (would go to -1)
    - isAgingCrisis(5, 3) returns false (still above 0)
    - isAgingCrisis(1, 1) returns true (would reach 0)
    - AGING_TABLE has exactly 4 brackets (34, 46, 58, 70)
  </behavior>
  <action>
    Read the Core Rulebook PDF for the aging table (Chapter 1, around p.47-48 in the character creation section). Transcribe exact aging check targets per age bracket.

    Create src/data/aging.ts:
    - Export AGING_TABLE as array of { minAge: number, checks: { characteristic: CharacteristicId, target: number }[] }
    - Four brackets: 34-45, 46-57, 58-69, 70+
    - Each bracket checks STR, DEX, END with increasing difficulty

    Create src/engine/aging.ts with pure functions:
    - getAgingChecks(age: number): { characteristic: CharacteristicId; target: number }[] | null — returns null if age < 34, otherwise finds correct bracket (AGNG-01)
    - resolveAgingCheck(diceTotal: number, dm: number, target: number): { reduced: boolean; amount: number } — if total < target, amount = target - total (AGNG-02)
    - isAgingCrisis(currentValue: number, reduction: number): boolean — true if currentValue - reduction <= 0 (AGNG-03)

    Create tests/engine/aging.test.ts testing all functions with edge cases for each bracket boundary and crisis detection.
  </action>
  <verify>
    <automated>npx vitest run tests/engine/aging.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "getAgingChecks" src/engine/aging.ts returns at least 1
    - grep -c "resolveAgingCheck" src/engine/aging.ts returns at least 1
    - grep -c "isAgingCrisis" src/engine/aging.ts returns at least 1
    - grep -c "AGING_TABLE" src/data/aging.ts returns at least 1
    - npx vitest run tests/engine/aging.test.ts exits with code 0
  </acceptance_criteria>
  <done>Aging engine handles all 4 age brackets, crisis detection at 0, and all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create mustering-out engine, life events, draft table, and tests</name>
  <files>src/engine/mustering-out.ts, src/data/life-events.ts, src/data/draft.ts, tests/engine/mustering-out.test.ts</files>
  <read_first>src/types/careers.ts, src/types/common.ts, "MgT 2E - Core Rulebook (Printer Friendly).pdf"</read_first>
  <behavior>
    - calculateBenefitRolls(termsServed=3, mishapTerm=false) returns 3; with mishapTerm=true returns 2 (MSTR-04)
    - getRankBonusRolls(rank=0) returns 0; rank=1 returns 1; rank=3 returns 2; rank=5 returns 3 (MSTR-02)
    - getRankDM(rank=5) returns 1; rank=4 returns 0 (MSTR-02: DM+1 for rank 5-6)
    - canRollCash(cashRollsUsed=2) returns true; canRollCash(3) returns false (MSTR-01: max 3)
    - calculatePension(totalTerms=4) returns 0; calculatePension(5) returns 10000; calculatePension(6) returns 12000 (MSTR-03)
    - getCombinedRank(enlistedRank=3, officerRank=2) returns 5 (MSTR-05)
    - LIFE_EVENTS has entries for rolls 2-12 (11 entries)
    - DRAFT_TABLE has entries for rolls 1-6 mapping to career names
  </behavior>
  <action>
    Read the Core Rulebook PDF for mustering out tables (cash/benefits per career are in each career's JSON — this file handles the RULES), life events table, draft table, and pension table.

    Create src/data/life-events.ts:
    - Export LIFE_EVENTS as array of { rollValue: number, description: string, effectDescription: string, effects: EventEffect[] }
    - 11 entries for 2D rolls (2-12). Read from Core Rulebook "Life Events" table.
    - Import EventEffect type from types/careers.ts

    Create src/data/draft.ts:
    - Export DRAFT_TABLE: Record<number, CareerName> mapping 1-6 to careers
    - 1=Navy, 2=Army, 3=Marines, 4=Merchant, 5=Scout, 6=Agent (verify against rulebook)

    Create src/engine/mustering-out.ts with pure functions:
    - calculateBenefitRolls(termsServed: number, mishapTerm: boolean): number — terms minus 1 if mishap on final term (MSTR-04)
    - getRankBonusRolls(rank: number): number — 1-2: +1 roll, 3-4: +2 rolls, 5-6: +3 rolls (MSTR-02)
    - getRankDM(rank: number): number — returns +1 for rank 5-6, 0 otherwise (MSTR-02)
    - canRollCash(cashRollsUsed: number): boolean — false if >= 3 (MSTR-01)
    - calculatePension(totalTerms: number): number — 0 if < 5; Cr10,000 for 5 terms + Cr2,000 per additional term (MSTR-03)
    - getCombinedRank(enlistedRank: number, officerRank: number): number — sum for benefit calculation (MSTR-05)

    Create tests/engine/mustering-out.test.ts with tests for all functions, including edge cases for max cash rolls, pension thresholds, mishap deduction.

    Per SOCL-01: Contact tracking is a store concern (Plan 04). Life events data here just declares when contacts are created.
  </action>
  <verify>
    <automated>npx vitest run tests/engine/mustering-out.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "calculateBenefitRolls" src/engine/mustering-out.ts returns at least 1
    - grep -c "calculatePension" src/engine/mustering-out.ts returns at least 1
    - grep -c "canRollCash" src/engine/mustering-out.ts returns at least 1
    - grep -c "LIFE_EVENTS" src/data/life-events.ts returns at least 1
    - grep -c "DRAFT_TABLE" src/data/draft.ts returns at least 1
    - npx vitest run tests/engine/mustering-out.test.ts exits with code 0
  </acceptance_criteria>
  <done>Mustering out engine handles all benefit calculation rules. Life events and draft table data transcribed from rulebook. All tests pass.</done>
</task>

</tasks>

<verification>
- npx vitest run tests/engine/aging.test.ts tests/engine/mustering-out.test.ts --reporter=verbose — all tests pass
- grep "export function" src/engine/aging.ts — at least 3 exported functions
- grep "export function" src/engine/mustering-out.ts — at least 5 exported functions
- grep "LIFE_EVENTS" src/data/life-events.ts — data exported
</verification>

<success_criteria>
- Aging engine correctly identifies which checks to make at each age bracket
- Aging crisis detection works when characteristic would reach 0
- Mustering out benefit rolls account for rank bonuses, cash limit, mishap deduction, and pension
- Life events table has all 11 entries (rolls 2-12)
- Draft table maps 1D roll to 6 careers
- All engine functions are pure with no side effects
</success_criteria>

<output>
After completion, create `.planning/phases/03-career-lifecycle/03-02-SUMMARY.md`
</output>
