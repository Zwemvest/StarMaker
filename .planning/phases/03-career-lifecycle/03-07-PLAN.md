---
phase: 03-career-lifecycle
plan: 07
type: execute
wave: 4
depends_on: [03-05, 03-06]
files_modified:
  - src/components/career/ContinueLeaveCard.tsx
  - src/components/career/AgingCard.tsx
  - src/components/career/TermTimeline.tsx
  - src/components/career/TermSummary.tsx
  - src/components/career/CareerStep.tsx
  - src/components/mustering-out/MusteringOutStep.tsx
  - src/components/mustering-out/BenefitRoll.tsx
  - src/components/mustering-out/PensionSummary.tsx
autonomous: true
requirements:
  - AGNG-01
  - AGNG-02
  - AGNG-03
  - MSTR-01
  - MSTR-02
  - MSTR-03
  - MSTR-04
  - MSTR-05
  - CRER-13
  - CRER-14
  - CRER-23
  - CRER-24

must_haves:
  truths:
    - "User sees continue/leave decision card with rank, terms, age, pension info"
    - "Aging effects trigger at 34+ with characteristic reductions shown"
    - "Vertical timeline shows completed terms collapsed with current term expanded"
    - "Mustering out rolls benefits one at a time with cash table limit of 3"
    - "Pension displayed for 5+ terms of service"
  artifacts:
    - path: "src/components/career/ContinueLeaveCard.tsx"
      provides: "Continue/leave decision per D-07"
      contains: "ContinueLeaveCard"
    - path: "src/components/career/AgingCard.tsx"
      provides: "Aging warning/crisis per D-15"
      contains: "AgingCard"
    - path: "src/components/career/TermTimeline.tsx"
      provides: "Vertical timeline per D-02"
      contains: "TermTimeline"
    - path: "src/components/mustering-out/MusteringOutStep.tsx"
      provides: "Mustering out orchestrator per D-14"
      contains: "MusteringOutStep"
  key_links:
    - from: "src/components/career/AgingCard.tsx"
      to: "src/engine/aging.ts"
      via: "aging engine functions"
      pattern: "getAgingChecks|resolveAgingCheck|isAgingCrisis"
    - from: "src/components/mustering-out/MusteringOutStep.tsx"
      to: "src/engine/mustering-out.ts"
      via: "mustering out engine"
      pattern: "calculateBenefitRolls|calculatePension"
    - from: "src/components/career/ContinueLeaveCard.tsx"
      to: "src/stores/character.ts"
      via: "career tracking data"
      pattern: "useCharacterStore"
---

<objective>
Build the continue/leave decision, aging effects, vertical timeline, and complete mustering out UI.

Purpose: These components complete the career lifecycle. The continue/leave card creates the "push your luck" tension (D-07). Aging adds real stakes at 34+ (D-15). The timeline provides biography context (D-02). Mustering out converts career service into tangible rewards (D-14). This plan also wires the term loop into CareerStep.
Output: ContinueLeaveCard, AgingCard, TermTimeline, TermSummary, MusteringOutStep, BenefitRoll, PensionSummary, and updated CareerStep.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-career-lifecycle/03-CONTEXT.md
@.planning/phases/03-career-lifecycle/03-02-SUMMARY.md
@.planning/phases/03-career-lifecycle/03-04-SUMMARY.md
@.planning/phases/03-career-lifecycle/03-05-SUMMARY.md
@.planning/phases/03-career-lifecycle/03-06-SUMMARY.md

<interfaces>
From src/engine/aging.ts (Plan 02):
```typescript
export function getAgingChecks(age: number): { characteristic: CharacteristicId; target: number }[] | null;
export function resolveAgingCheck(diceTotal: number, dm: number, target: number): { reduced: boolean; amount: number };
export function isAgingCrisis(currentValue: number, reduction: number): boolean;
```

From src/engine/mustering-out.ts (Plan 02):
```typescript
export function calculateBenefitRolls(termsServed: number, mishapTerm: boolean): number;
export function getRankBonusRolls(rank: number): number;
export function getRankDM(rank: number): number;
export function canRollCash(cashRollsUsed: number): boolean;
export function calculatePension(totalTerms: number): number;
export function getCombinedRank(enlistedRank: number, officerRank: number): number;
```

From src/machines/creation.ts (Plan 04):
```typescript
// Relevant events:
| { type: 'AGING_RESOLVED' }
| { type: 'CONTINUE_CAREER' }
| { type: 'CHANGE_CAREER' }
| { type: 'MUSTER_OUT' }
| { type: 'BENEFIT_ROLLED' }
| { type: 'MUSTERING_COMPLETE' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContinueLeaveCard, AgingCard, TermTimeline, and TermSummary</name>
  <files>src/components/career/ContinueLeaveCard.tsx, src/components/career/AgingCard.tsx, src/components/career/TermTimeline.tsx, src/components/career/TermSummary.tsx</files>
  <read_first>src/engine/aging.ts, src/engine/career.ts, src/stores/character.ts, src/machines/creation.ts, src/hooks/useLoggedRoll.ts, src/types/careers.ts</read_first>
  <action>
    Create src/components/career/ContinueLeaveCard.tsx (per D-07):
    - Props: currentRank: string, termsInCareer: number, totalTerms: number, age: number, forcedToLeave: boolean, forcedToStay: boolean, onContinue: () => void, onChangeCareer: () => void, onMusterOut: () => void
    - Shows current rank, terms served, current age
    - If age >= 30 (approaching 34): amber warning "Aging effects begin at 34"
    - If age >= 34: red warning "Aging check required each term" (AGNG-01)
    - Pension info per D-16:
      - At 4 terms: "One more term qualifies for Cr10,000/year pension!"
      - At 5+ terms: shows current pension amount from calculatePension
    - If forcedToLeave (CRER-13): only "Muster Out" button available, message "Your career has stalled. You must leave."
    - If forcedToStay (CRER-14): only "Serve Another Term" button, message "Your career is thriving! You must continue."
    - Normal: Three buttons: "Serve Another Term" (same career), "Change Career" (CRER-22, CRER-23), "Muster Out"
    - "Change Career" uses resolveAssignmentChange from engine/career.ts (CRER-23): if user picks same career with different assignment, keep rank and skip qualification; if new career, require new qualification and reset rank
    - "Change Career" note: shows DM-1 penalty warning (CRER-22)
    - Cannot return to same career shown as disabled option note (CRER-24)
    - Dispatches CONTINUE_CAREER, CHANGE_CAREER, or MUSTER_OUT

    Create src/components/career/AgingCard.tsx (per D-15):
    - Props: age: number, characteristics: Characteristics, onResolved: () => void
    - Uses getAgingChecks(age) to determine checks needed
    - If null (age < 34): skip, should not render
    - For each check (STR, DEX, END): roll 2D using useLoggedRoll with context "Aging Check ({stat})"
    - Call resolveAgingCheck for each
    - Show results: "STR: Passed" (green) or "STR: -2" (red)
    - If any reduction: call store.reduceCharacteristic for each
    - If isAgingCrisis: dramatic red "AGING CRISIS!" display — characteristic reached 0 (AGNG-03). Show that character must receive medical care or die (per rulebook, this is a medical check in gameplay — for creation purposes, note the crisis and continue)
    - After all checks displayed: "Continue" button dispatches AGING_RESOLVED

    Create src/components/career/TermTimeline.tsx (per D-02):
    - Props: careerHistory: CareerTerm[], currentTerm?: { career, assignment, rank, termNumber }
    - Vertical timeline layout with completed terms as collapsed summary rows
    - Current term expanded with details
    - Each completed term shows: term number, career name, rank achieved, key events (1-line summaries)
    - Scrolls naturally for long career histories
    - Uses Tailwind for timeline line + dots pattern

    Create src/components/career/TermSummary.tsx:
    - Props: term: CareerTerm, isExpanded: boolean, onToggle: () => void
    - Collapsed: single row with career name, rank, term number
    - Expanded: shows skills gained, events, survival status
    - Click to toggle expand/collapse
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ContinueLeaveCard" src/components/career/ContinueLeaveCard.tsx returns at least 1
    - grep -c "forcedToLeave" src/components/career/ContinueLeaveCard.tsx returns at least 1
    - grep -c "forcedToStay" src/components/career/ContinueLeaveCard.tsx returns at least 1
    - grep -c "calculatePension" src/components/career/ContinueLeaveCard.tsx returns at least 1
    - grep -c "AgingCard" src/components/career/AgingCard.tsx returns at least 1
    - grep -c "getAgingChecks" src/components/career/AgingCard.tsx returns at least 1
    - grep -c "isAgingCrisis" src/components/career/AgingCard.tsx returns at least 1
    - grep -c "TermTimeline" src/components/career/TermTimeline.tsx returns at least 1
  </acceptance_criteria>
  <done>Continue/leave card shows rank, age, pension with forced leave/stay logic. Aging card rolls checks and shows reductions with crisis detection. Timeline shows career biography.</done>
</task>

<task type="auto">
  <name>Task 2: Create MusteringOutStep and wire term loop into CareerStep</name>
  <files>src/components/mustering-out/MusteringOutStep.tsx, src/components/mustering-out/BenefitRoll.tsx, src/components/mustering-out/PensionSummary.tsx, src/components/career/CareerStep.tsx</files>
  <read_first>src/components/career/CareerStep.tsx, src/engine/mustering-out.ts, src/stores/character.ts, src/machines/creation.ts, src/data/careers/index.ts, src/hooks/useLoggedRoll.ts</read_first>
  <action>
    Create src/components/mustering-out/BenefitRoll.tsx (per D-14):
    - Props: career: CareerData, rollNumber: number, totalRolls: number, cashRollsUsed: number, rankDM: number, onRolled: (type: 'cash' | 'benefits') => void
    - Shows "Roll {rollNumber} of {totalRolls}"
    - Two buttons: "Cash Table" and "Benefits Table"
    - Cash Table button disabled if cashRollsUsed >= 3 (MSTR-01), shows "(Max 3 reached)"
    - On Cash: roll 1D + gambler skill DM, lookup career.musteringOut.cash[rollValue-1], add credits to store
    - On Benefits: roll 1D + rankDM (MSTR-02: DM+1 for rank 5-6), lookup career.musteringOut.benefits[rollValue-1], add benefit to store
    - Show result inline: "Cr25,000" or "Weapon" etc.
    - Uses useLoggedRoll().roll1D with context "Mustering Out {Cash/Benefits}"

    Create src/components/mustering-out/PensionSummary.tsx (per D-16):
    - Props: totalTerms: number, totalCredits: number, benefits: string[], pension: number
    - Summary card showing:
      - Total credits earned
      - List of benefit items received
      - If totalTerms >= 5: "Annual Pension: Cr{pension}/year" prominently displayed (MSTR-03)
    - "Complete Character Creation" button dispatches MUSTERING_COMPLETE

    Create src/components/mustering-out/MusteringOutStep.tsx:
    - Orchestrator for mustering out flow
    - Calculates total benefit rolls per career: calculateBenefitRolls(terms, mishapTerm) + getRankBonusRolls(rank) (MSTR-02)
    - For military with officer rank: getCombinedRank(enlisted, officer) for bonus rolls (MSTR-05)
    - Lost benefit on mishap term: one fewer roll per MSTR-04
    - Shows BenefitRoll cards sequentially, one at a time
    - After all rolls: shows PensionSummary
    - Calculates and sets pension via calculatePension (MSTR-03)

    Update src/components/career/CareerStep.tsx:
    - Wire the term loop sub-states to the components from Plans 05-07:
      - "termLoop.survivalRoll" -> <SurvivalRoll />
      - "termLoop.mishap" -> <MishapCard />
      - "termLoop.event" -> <CareerEventCard />
      - "termLoop.commission" -> <CommissionCard />
      - "termLoop.advancement" -> <AdvancementCard />
      - "termLoop.skillSelection" -> <SkillTableTabs />
      - "termLoop.aging" -> <AgingCard />
      - "termLoop.continueOrLeave" -> <ContinueLeaveCard />
      - "musteringOut" -> <MusteringOutStep />
    - Remove placeholder renders added in Plan 05
    - Wrap term components with <TermTimeline /> for context
    - After each term completion, call store.addCareerTerm() with term record
    - Update store.setAge(age + 4) each term
    - Track career changes with store.addPreviousCareer() and store.setLastCareer()
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "MusteringOutStep" src/components/mustering-out/MusteringOutStep.tsx returns at least 1
    - grep -c "calculateBenefitRolls" src/components/mustering-out/MusteringOutStep.tsx returns at least 1
    - grep -c "BenefitRoll" src/components/mustering-out/BenefitRoll.tsx returns at least 1
    - grep -c "canRollCash" src/components/mustering-out/BenefitRoll.tsx returns at least 1
    - grep -c "PensionSummary" src/components/mustering-out/PensionSummary.tsx returns at least 1
    - grep -c "calculatePension" src/components/mustering-out/PensionSummary.tsx returns at least 1
    - grep -c "SurvivalRoll" src/components/career/CareerStep.tsx returns at least 1
    - grep -c "MusteringOutStep" src/components/career/CareerStep.tsx returns at least 1
    - grep -c "TermTimeline" src/components/career/CareerStep.tsx returns at least 1
  </acceptance_criteria>
  <done>Mustering out shows roll-by-roll benefits with cash limit and pension. CareerStep fully wired with all term loop components and timeline. Complete career lifecycle functional.</done>
</task>

</tasks>

<verification>
- npx vitest run — full suite passes
- Career term loop flows through all sub-states: survival -> event -> commission/advancement -> skill -> aging -> continue/leave
- Mustering out rolls benefits correctly with rank bonuses
- Pension shown for 5+ terms
- Aging triggers at 34+ with visible characteristic reductions
- Timeline shows career history
</verification>

<success_criteria>
- Continue/leave card shows rank, terms, age, pension eligibility with correct forced-leave/stay handling (D-07, CRER-13, CRER-14)
- Aging card shows checks at 34+ with crisis detection (D-15, AGNG-01, AGNG-02, AGNG-03)
- Timeline shows completed terms collapsed with current expanded (D-02)
- Mustering out is roll-by-roll with cash limit of 3 and rank bonuses (D-14, MSTR-01, MSTR-02)
- Pension calculated for 5+ terms (D-16, MSTR-03)
- Mishap term loses benefit roll (MSTR-04)
- Combined rank used for military benefits (MSTR-05)
- CareerStep fully wired with all components from Plans 05-07
</success_criteria>

<output>
After completion, create `.planning/phases/03-career-lifecycle/03-07-SUMMARY.md`
</output>
