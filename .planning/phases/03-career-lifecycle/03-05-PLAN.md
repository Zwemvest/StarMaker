---
phase: 03-career-lifecycle
plan: 05
type: execute
wave: 3
depends_on: [03-03, 03-04]
files_modified:
  - src/components/career/CareerStep.tsx
  - src/components/career/CareerGrid.tsx
  - src/components/career/AssignmentCards.tsx
  - src/components/career/QualFailCard.tsx
  - src/components/career/BasicTrainingCard.tsx
autonomous: true
requirements:
  - CRER-01
  - CRER-02
  - CRER-03
  - CRER-04
  - CRER-05
  - CRER-20
  - CRER-21
  - CRER-22

must_haves:
  truths:
    - "User sees 12 career cards in a grid with qualification info and DM penalties"
    - "User can select a career and choose from 3 assignments"
    - "Failed qualification shows Draft or Drifter choice"
    - "Basic training shows correct skills (service skills for first career, pick-one for subsequent, assignment skills for Citizen/Drifter)"
  artifacts:
    - path: "src/components/career/CareerStep.tsx"
      provides: "Main career step orchestrator"
      contains: "CareerStep"
    - path: "src/components/career/CareerGrid.tsx"
      provides: "12-career selection grid per D-10"
      contains: "CareerGrid"
    - path: "src/components/career/AssignmentCards.tsx"
      provides: "3-assignment sub-cards per D-11"
      contains: "AssignmentCards"
    - path: "src/components/career/QualFailCard.tsx"
      provides: "Qualification failure choice per D-12"
      contains: "QualFailCard"
    - path: "src/components/career/BasicTrainingCard.tsx"
      provides: "Basic training skill display"
      contains: "BasicTrainingCard"
  key_links:
    - from: "src/components/career/CareerStep.tsx"
      to: "src/machines/creation.ts"
      via: "useCreationMachine for sub-state routing"
      pattern: "useCreationMachine"
    - from: "src/components/career/CareerGrid.tsx"
      to: "src/data/careers/index.ts"
      via: "CAREERS data for grid display"
      pattern: "CAREERS|getCareer"
    - from: "src/components/career/CareerStep.tsx"
      to: "src/stores/character.ts"
      via: "store for DM calculations"
      pattern: "useCharacterStore"
---

<objective>
Build the career selection UI: career grid, assignment selection, qualification failure handling, and basic training display.

Purpose: This is the entry point to careers. Users see all 12 careers, pick one, choose an assignment, roll for qualification, handle failures, and receive basic training. Per D-10, D-11, D-12, these use card-based UI patterns.
Output: CareerStep orchestrator, CareerGrid, AssignmentCards, QualFailCard, BasicTrainingCard components.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-career-lifecycle/03-CONTEXT.md
@.planning/phases/03-career-lifecycle/03-01-SUMMARY.md
@.planning/phases/03-career-lifecycle/03-03-SUMMARY.md
@.planning/phases/03-career-lifecycle/03-04-SUMMARY.md

<interfaces>
From src/hooks/useCreationMachine.ts:
```typescript
export function useCreationMachine(): { state, send, subState };
```

From src/hooks/useLoggedRoll.ts:
```typescript
export function useLoggedRoll(): { roll2D, roll1D };
```

From src/data/careers/index.ts (Plan 03):
```typescript
export const CAREERS: Record<CareerName, CareerData>;
export function getCareer(name: CareerName): CareerData;
export const ALL_CAREER_NAMES: readonly CareerName[];
```

From src/engine/career.ts (Plan 01):
```typescript
export function calculateQualificationDM(previousCareersCount: number): number;
export function resolveQualificationRoll(diceTotal: number, dm: number, target: number): { success: boolean; total: number };
export function getBasicTrainingSkills(career: CareerData, isFirstCareer: boolean): string[];
export function getDraftCareer(rollValue: number): CareerName;
```

From src/machines/creation.ts (Plan 04):
```typescript
// Career events:
| { type: 'CHOOSE_CAREER'; career: CareerName }
| { type: 'CHOOSE_ASSIGNMENT'; assignment: string }
| { type: 'QUALIFICATION_SUCCESS' }
| { type: 'QUALIFICATION_FAILURE' }
| { type: 'CHOOSE_DRAFT' }
| { type: 'CHOOSE_DRIFTER' }
| { type: 'BASIC_TRAINING_COMPLETE' }
```

From src/components/ui/ (existing):
```typescript
// Button, Card components available for reuse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CareerGrid and AssignmentCards components</name>
  <files>src/components/career/CareerGrid.tsx, src/components/career/AssignmentCards.tsx</files>
  <read_first>src/data/careers/index.ts, src/engine/career.ts, src/types/careers.ts, src/stores/character.ts, src/components/education/EducationStep.tsx, src/components/ui/Card.tsx</read_first>
  <action>
    Create src/components/career/CareerGrid.tsx (per D-10):
    - Import CAREERS, ALL_CAREER_NAMES from data/careers/index.ts
    - Import useCharacterStore to get previousCareers and lastCareer for DM calculation
    - Import characteristicModifier from types/common.ts
    - Import calculateQualificationDM from engine/career.ts
    - Display 12 career cards in a responsive grid (4 cols on lg, 3 on md, 2 on sm per Claude's discretion)
    - Each card shows:
      - Career name (bold)
      - Qualification: "{characteristic} {target}+" with calculated user DM
      - 3 assignment names as preview
      - DM-1 penalty indicator if previousCareers.length > 0 (CRER-03)
      - Locked/grayed out if lastCareer matches (CRER-24: cannot return immediately)
    - Drifter card shows "No Qualification Required" instead of target
    - onClick dispatches CHOOSE_CAREER event with career name
    - Props: onChoose: (career: CareerName) => void

    Create src/components/career/AssignmentCards.tsx (per D-11):
    - Props: career: CareerData, onChoose: (assignment: string) => void
    - Display 3 assignment cards below the selected career
    - Each card shows:
      - Assignment name (bold)
      - Assignment description
      - Survival: "{characteristic} {target}+"
      - Advancement: "{characteristic} {target}+"
      - Specialist skills preview (6 skills listed)
    - onClick dispatches CHOOSE_ASSIGNMENT event
    - Tailwind styling: scanner-blue border on hover, consistent with existing card patterns
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CareerGrid" src/components/career/CareerGrid.tsx returns at least 1
    - grep -c "CAREERS" src/components/career/CareerGrid.tsx returns at least 1
    - grep -c "calculateQualificationDM" src/components/career/CareerGrid.tsx returns at least 1
    - grep -c "AssignmentCards" src/components/career/AssignmentCards.tsx returns at least 1
    - grep -c "specialistSkills" src/components/career/AssignmentCards.tsx returns at least 1
  </acceptance_criteria>
  <done>CareerGrid shows 12 careers with qualification info and DM penalties. AssignmentCards shows 3 assignments with survival/advancement targets. Both dispatch correct XState events.</done>
</task>

<task type="auto">
  <name>Task 2: Create QualFailCard, BasicTrainingCard, and CareerStep orchestrator</name>
  <files>src/components/career/QualFailCard.tsx, src/components/career/BasicTrainingCard.tsx, src/components/career/CareerStep.tsx</files>
  <read_first>src/components/education/EducationStep.tsx, src/hooks/useCreationMachine.ts, src/hooks/useLoggedRoll.ts, src/engine/career.ts, src/machines/creation.ts</read_first>
  <action>
    Create src/components/career/QualFailCard.tsx (per D-12):
    - Shows "Qualification Failed" heading with red accent
    - Two option buttons:
      - "Submit to Draft" — explains random career assignment (CRER-20). Disabled and shows "(Already drafted)" if store.drafted is true.
      - "Become a Drifter" — explains automatic entry to Drifter career
    - Each option shows brief consequences
    - onDraft calls useLoggedRoll().roll1D for draft table, then dispatches CHOOSE_DRAFT
    - onDrifter dispatches CHOOSE_DRIFTER
    - Props: onDraft: () => void, onDrifter: () => void, alreadyDrafted: boolean

    Create src/components/career/BasicTrainingCard.tsx:
    - Shows skills granted during basic training
    - For first career: "You receive all service skills at level 0:" followed by skill list (CRER-04)
    - For subsequent careers: "Choose one service skill at level 0:" with clickable skill buttons
    - For Citizen/Drifter: uses assignment specialist skills instead (CRER-05)
    - Uses getBasicTrainingSkills from engine/career.ts
    - After skill assignment, dispatches BASIC_TRAINING_COMPLETE
    - Calls store.addSkill for each granted skill
    - Props: career: CareerData, isFirstCareer: boolean, onComplete: () => void

    Create src/components/career/CareerStep.tsx:
    - Main orchestrator component that renders based on XState sub-state
    - Uses useCreationMachine() to get state, send, subState
    - Uses useCharacterStore for characteristic DMs and career tracking
    - Sub-state routing:
      - "choosingCareer" -> <CareerGrid />
      - "choosingAssignment" -> <AssignmentCards /> with selected career
      - "qualificationRoll" -> Qualification roll card with useLoggedRoll, resolveQualificationRoll. On success: send QUALIFICATION_SUCCESS. On failure: send QUALIFICATION_FAILURE.
      - "qualificationFailed" -> <QualFailCard />
      - "basicTraining" -> <BasicTrainingCard />
      - "termLoop.*" -> delegate to term components (Plan 06, render placeholder for now)
      - "musteringOut" -> delegate to mustering out (Plan 07, render placeholder for now)
    - Qualification roll card follows D-05 pattern but for qualification: show target, DM, "Roll for Qualification" button
    - Wire into WizardShell by registering as the career step component
    - Add CareerStep to the wizard step registry (wherever EducationStep is registered)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CareerStep" src/components/career/CareerStep.tsx returns at least 1
    - grep -c "useCreationMachine" src/components/career/CareerStep.tsx returns at least 1
    - grep -c "QualFailCard" src/components/career/QualFailCard.tsx returns at least 1
    - grep -c "CHOOSE_DRAFT" src/components/career/QualFailCard.tsx returns at least 1
    - grep -c "BasicTrainingCard" src/components/career/BasicTrainingCard.tsx returns at least 1
    - grep -c "getBasicTrainingSkills" src/components/career/BasicTrainingCard.tsx returns at least 1
    - grep -c "choosingCareer" src/components/career/CareerStep.tsx returns at least 1
  </acceptance_criteria>
  <done>Career selection flow works end-to-end: grid -> assignment -> qualification roll -> failure handling -> basic training. CareerStep registered in wizard. Placeholder renders for term loop and mustering out (built in Plans 06-07).</done>
</task>

</tasks>

<verification>
- npx vitest run — full suite passes
- Career step renders in the wizard at the correct position
- CareerGrid displays all 12 careers with correct data
- QualFailCard shows draft and drifter options
- BasicTrainingCard handles first vs subsequent career and Citizen/Drifter exception
</verification>

<success_criteria>
- CareerGrid shows 12 careers with qualification targets, user DMs, and assignment previews (D-10)
- AssignmentCards shows 3 assignments with survival/advancement targets (D-11)
- QualFailCard offers Draft or Drifter choice (D-12)
- BasicTrainingCard grants correct skills for first/subsequent/exception careers (CRER-04, CRER-05)
- CareerStep routes to correct component based on XState sub-state
- CareerStep is registered in the wizard and accessible after education
</success_criteria>

<output>
After completion, create `.planning/phases/03-career-lifecycle/03-05-SUMMARY.md`
</output>
