---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - DEPL-01
  - DEPL-02

must_haves:
  truths:
    - "Pushing to main triggers a GitHub Actions workflow that builds and deploys to GitHub Pages"
    - "The deployed app loads at the GitHub Pages URL with correct asset paths"
    - "CI runs tests before deploying — failed tests block deployment"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions CI/CD pipeline"
      contains: "deploy-pages"
      min_lines: 30
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "npm run build"
      via: "Build step produces dist/ for deployment"
      pattern: "npm run build"
    - from: ".github/workflows/deploy.yml"
      to: "npx vitest run"
      via: "Test step gates deployment"
      pattern: "npm test|vitest"
---

<objective>
Create the GitHub Actions CI/CD pipeline that builds, tests, and deploys StarMaker to GitHub Pages on every push to main. This closes the deployment loop — code merged to main automatically appears at the live URL.

Purpose: Continuous deployment from day one means every phase delivers a visible, shippable increment. No manual deploy steps.
Output: Working GitHub Actions workflow deploying to GitHub Pages.
</objective>

<execution_context>
@C:/Users/XPRTZRikOostveen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/XPRTZRikOostveen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy.yml` with the following pipeline:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npx vitest run
      - name: Build
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: './dist'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

NOTE: Using `actions/checkout@v4`, `actions/setup-node@v4` (not v6 -- v4 is the current stable). Node 22 (LTS) for Vite 8 compatibility. Tests run BEFORE build to gate deployment.

After creating the workflow file, verify it's valid YAML by running a basic syntax check.
  </action>
  <verify>
    <automated>cat .github/workflows/deploy.yml | head -5 && echo "--- File exists and is readable ---"</automated>
  </verify>
  <done>deploy.yml exists with correct structure: checkout, install, test, build, upload, deploy steps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub Pages deployment</name>
  <action>
This is a human verification checkpoint. Claude has created the GitHub Actions workflow. The user must now verify the deployment works end-to-end.

What was built: GitHub Actions CI/CD pipeline for deploying StarMaker to GitHub Pages. The workflow runs tests, builds the app, and deploys to Pages on every push to main.

How to verify:

1. Ensure GitHub Pages is enabled for the repository:
   - Go to https://github.com/[your-username]/StarMaker/settings/pages
   - Under "Build and deployment" > Source, select "GitHub Actions"

2. Push the current code to the main branch (or trigger manually):
   - Go to https://github.com/[your-username]/StarMaker/actions
   - Verify the "Deploy to GitHub Pages" workflow runs
   - Check that the workflow completes successfully (green checkmark)

3. Visit the deployed site:
   - URL will be: https://[your-username].github.io/StarMaker/
   - Verify the app loads (should show "StarMaker" text on dark background)
   - Verify assets load correctly (no 404s in browser console)

4. If the workflow fails, check the logs for:
   - Node version issues (needs 22+)
   - Pages permission issues (needs id-token: write)
   - Build failures (check npm ci and build steps)
  </action>
  <verify>
    <automated>gh run list --workflow=deploy.yml --limit=1 2>/dev/null || echo "Check GitHub Actions manually"</automated>
  </verify>
  <done>GitHub Actions workflow runs successfully and the app is accessible at the GitHub Pages URL with correct styling and no 404 errors.</done>
</task>

</tasks>

<verification>
- `.github/workflows/deploy.yml` exists with valid YAML
- Workflow triggers on push to main and workflow_dispatch
- Pipeline includes: checkout, node setup, npm ci, test, build, pages setup, upload, deploy
- Tests gate deployment (run before build)
</verification>

<success_criteria>
1. GitHub Actions workflow file exists at .github/workflows/deploy.yml
2. Workflow runs tests before deploying (test failure = no deploy)
3. App is accessible at GitHub Pages URL after successful push
4. Static assets load with correct /StarMaker/ base path
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
