---
phase: 04-post-career-and-sheet
plan: 07
type: execute
wave: 5
depends_on: ['05', '06']
files_modified:
  - tests/engine/golden-path.test.ts
  - .planning/phases/04-post-career-and-sheet/04-HUMAN-UAT.md
  - .planning/phases/04-post-career-and-sheet/04-VERIFICATION.md
autonomous: false
requirements:
  - PSIN-01
  - PSIN-02
  - PSIN-03
  - PSIN-04
  - PSIN-05
  - PSIN-06
  - EQUP-01
  - EQUP-02
  - EQUP-03
  - EQUP-04
  - SHEE-01
  - SHEE-02
  - SHEE-03
  - SHEE-04
  - SHEE-05

must_haves:
  truths:
    - "A Phase-4 golden-path character runs end-to-end through the pure engines + store: psionics unlock (legitimate), PSI roll, Telepathy auto-grant + one rolled talent, weapon + armour purchase against mustering-out credits, reaching sheet/complete with a stable legitimacy hash"
    - "A second golden character uses the psionics force-unlock door and asserts isModified flips from false to true with a logged modification entry"
    - "Every Phase 4 requirement (PSIN-01..06, EQUP-01..04, SHEE-01..05) is mapped to the test(s)/component(s) that satisfy it"
    - "The full vitest suite passes (npx vitest run) and tsc --noEmit is clean"
    - "A human UAT script exists covering both psionics paths (skip + force-unlock), budgeted equipment purchase, final sheet review, and print/PDF — with explicit expected results and badge checks per step"
  artifacts:
    - path: "tests/engine/golden-path.test.ts"
      provides: "Phase-4 end-to-end golden character(s) extending the existing GP1-GP6 suite"
      contains: "GP7"
    - path: ".planning/phases/04-post-career-and-sheet/04-HUMAN-UAT.md"
      provides: "Manual test script for the full post-career flow per Phase 4 SC 1-5"
      contains: "Legitimate"
    - path: ".planning/phases/04-post-career-and-sheet/04-VERIFICATION.md"
      provides: "Pass/fail summary against the five roadmap success criteria"
      contains: "Success Criteria"
  key_links:
    - from: "tests/engine/golden-path.test.ts"
      to: "src/engine/psionics.ts"
      via: "PSI roll + talent learning"
      pattern: "rollPsiTest|learnTalent|TELEPATHY"
    - from: "tests/engine/golden-path.test.ts"
      to: "src/engine/equipment.ts"
      via: "budgeted purchase"
      pattern: "purchaseEquipment|canAfford"
    - from: "tests/engine/golden-path.test.ts"
      to: "src/stores/character.ts"
      via: "store-driven assertions"
      pattern: "useCharacterStore"
    - from: "tests/engine/golden-path.test.ts"
      to: "src/engine/hash.ts"
      via: "stable legitimacy hash"
      pattern: "computeHash"
---

<objective>
Verify Phase 4 (post-career & character sheet) end-to-end. Extend the golden-path
test file with a deterministic Phase-4 character that exercises psionics, equipment
purchase, and the final sheet, plus a second character that proves the force-unlock
→ Modified door works. Map every Phase 4 requirement to its satisfying test/component,
gate on a green full suite + clean typecheck, produce a human UAT script, and write
the phase verification report.

Purpose: 04-07 is the verification + UAT capstone for Phase 4. It does not build new
features — it proves the features delivered by 04-01..04-06 satisfy the five roadmap
success criteria and the 15 requirements (PSIN-01..06, EQUP-01..04, SHEE-01..05).
Output: Phase-4 golden-path tests, requirement traceability, human UAT script,
verification report.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-post-career-and-sheet/04-DESIGN.md
@.planning/phases/04-post-career-and-sheet/04-05-SUMMARY.md
@.planning/phases/04-post-career-and-sheet/04-06-SUMMARY.md

<dependencies>
This plan runs in **wave 5** and depends on the implemented post-career UI (04-05)
and the character sheet + export (04-06), which in turn depend on the types/data
(04-01, 04-02), engines (04-03), and store + machine (04-04). Before writing tests,
**read the actual delivered exports** — do not assume the names below verbatim.

The golden-path tests are **pure engine + store** (no React, no DOM). They must be
deterministic: every dice value is supplied explicitly, never randomly rolled.
</dependencies>

<interfaces>
Verified existing (do NOT change):
```typescript
// src/engine/hash.ts
export function canonicalizeRollLog(entries: readonly RollLogEntry[]): string;
export async function computeHash(entries: readonly RollLogEntry[]): Promise<string>; // 8-char hex

// src/stores/character.ts — useCharacterStore (Zustand)
//   state:  characteristics, skills, rollLog, legitimacyHash, isModified,
//           careerHistory, contacts, age, cashRollsUsed, credits, pension,
//           benefits, creationPhase
//   actions: setLegitimacyHash, setModified, addCredits, appendRoll,
//            resetCharacter, setCreationPhase, ...
```

Delivered by Phase 4 (04-03 engines / 04-04 store — confirm exact names on read):
```typescript
// src/engine/psionics.ts (04-03)
//   PSI = 2D - terms served (PSIN-01); talent DMs Telepathy +4 ... Teleportation +0
//   (PSIN-02); cumulative -1 per prior attempt (PSIN-03); first-chosen Telepathy
//   auto-granted with no roll (PSIN-04). Talents carry their powers (PSIN-05/06).
//   Likely: rollPsiTest(diceTotal, termsServed), learnTalent(...), TALENT_DMS,
//           TELEPATHY auto-grant helper.

// src/engine/equipment.ts (04-03)
//   purchase/budget: spend mustering-out credits, clamp to balance, cannot
//   overspend (EQUP-03). Likely: canAfford(item, credits), purchaseEquipment(...).

// src/engine/legitimacy.ts (04-03) + store
//   force-unlock flips legitimacy legitimate -> modified and logs an entry (D-3, SHEE-05).

// src/engine/unusual-events.ts (04-03/04-04)
//   resolves the Life-Events roll-12 Unusual-Event 1D sub-table; result 1
//   sets psionicsUnlocked = true (legitimate path).

// src/data/ (04-01/04-02)
//   psionic talents table; Unusual-Events 1D sub-table; full equipment catalog
//   (6 categories) consumed by getCareer-style accessors.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend golden-path tests with the Phase-4 end-to-end character(s)</name>
  <files>tests/engine/golden-path.test.ts</files>
  <read_first>tests/engine/golden-path.test.ts, src/engine/psionics.ts, src/engine/equipment.ts, src/engine/legitimacy.ts, src/engine/unusual-events.ts, src/engine/hash.ts, src/stores/character.ts, src/data/psionics.ts, src/data/equipment (catalog files), src/data/unusual-events.ts</read_first>
  <behavior>
    - GP7 (legitimate, full Phase-4 path): a deterministic character that (a) gets the
      Unusual-Event psionics unlock via the roll-12 1D sub-table result 1 — legitimate,
      isModified stays false; (b) rolls PSI = 2D − terms served with supplied dice and
      learns Telepathy (auto-grant, no roll, PSIN-04) plus one rolled talent that
      succeeds with its DM + PSI DM + cumulative −1 (PSIN-02/03); (c) buys at least one
      weapon and one armour from the catalog against its mustering-out credits, with the
      credit balance decremented and never going negative (EQUP-03); (d) reaches the
      sheet/complete state and produces a legitimacy hash via computeHash that is stable
      (same inputs → same 8-char hash) and matches the badge state Legitimate.
    - GP8 (force-unlock / honest-cheat door): a character that is NOT unlocked, elects
      "Test anyway", and asserts isModified flips false → true with a logged
      modification entry, and that the badge would read Modified (D-2, D-3, SHEE-05).
    - Determinism: no random rolls. Reset the store before each store-driven test.
  </behavior>
  <action>
    Append to tests/engine/golden-path.test.ts (do not delete GP1-GP6). Add the store
    import at top alongside the existing engine imports:

    ```typescript
    import { useCharacterStore } from '../../src/stores/character';
    import { computeHash } from '../../src/engine/hash';
    // + the actual Phase-4 engine exports you confirmed on read, e.g.:
    // import { rollPsiTest, learnTalent, TALENT_DMS } from '../../src/engine/psionics';
    // import { canAfford, purchaseEquipment } from '../../src/engine/equipment';
    // import { resolveUnusualEvent } from '../../src/engine/unusual-events';
    ```

    **describe('GP7 — Psionic + equipped character (Phase-4 golden path)')**

    1. Unusual-Event unlock (legitimate): call the roll-12 1D sub-table resolver with a
       supplied `1` and assert it sets psionicsUnlocked = true. Assert that reaching the
       psionics step this way leaves isModified === false.
    2. PSI roll (PSIN-01): with terms served = 3 and a supplied 2D total of 10, assert
       PSI === 10 − 3 === 7. (Use the exact engine signature; if it returns a clamped
       1..15 value, assert against the rulebook clamp.)
    3. Telepathy auto-grant (PSIN-04): assert learning Telepathy as the first talent
       grants it with NO roll consumed and no cumulative penalty applied yet.
    4. One rolled talent (PSIN-02/03): attempt a second talent (e.g. Clairvoyance) with
       a supplied dice value; assert the effective DM = talent DM + PSI DM + (−1 × prior
       attempts) and that the success/failure matches the rulebook target. Verify the
       cumulative −1 increments on the next attempt.
    5. Talent powers (PSIN-05/06): assert the acquired talents carry their powers
       (cost/reach) from the data table.
    6. Equipment purchase (EQUP-01/02/03): seed the store with mustering-out credits via
       addCredits(N). Pick one weapon and one armour from the catalog whose combined cost
       ≤ N. Assert canAfford is true, purchase decrements credits by exactly the combined
       cost, the items are owned, and a deliberate over-budget purchase is rejected
       (credits unchanged, never negative).
    7. Sheet/complete + stable hash (SHEE-02/03/04): drive the store to the terminal
       phase (setCreationPhase('complete') or the delivered equivalent), compute the hash
       twice over the same rollLog via computeHash, assert both calls return the identical
       8-char hex string (stable/deterministic), and assert isModified === false so the
       sheet badge reads Legitimate (SHEE-05).

    **describe('GP8 — Force-unlock flips Modified (Phase-4 golden path)')**

    1. resetCharacter(); assert isModified === false (baseline Legitimate).
    2. With psionicsUnlocked === false, invoke the force-unlock action/engine path
       ("Test anyway").
    3. Assert isModified === true after force-unlock (SHEE-05, D-2/D-3).
    4. Assert a modification log entry was recorded (per the delivered legitimacy
       machinery — check the store field/array the engine appends to).
    5. Assert the badge derivation would read Modified (whatever selector/derivation
       04-06 exposes; if it is a pure helper, call it; otherwise assert isModified).

    Keep assertions tied to real rulebook values from 04-DESIGN / the Core Rulebook
    psionics + equipment tables. Use `beforeEach(() => useCharacterStore.getState().resetCharacter())`
    inside the store-driven describes so GP7/GP8 do not leak state into each other or
    into the existing pure-data tests.
  </action>
  <verify>
    <automated>npx vitest run tests/engine/golden-path.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "GP7" tests/engine/golden-path.test.ts returns at least 1
    - grep -c "GP8" tests/engine/golden-path.test.ts returns at least 1
    - grep -c "useCharacterStore" tests/engine/golden-path.test.ts returns at least 1
    - grep -c "computeHash" tests/engine/golden-path.test.ts returns at least 1
    - grep -Ec "isModified" tests/engine/golden-path.test.ts returns at least 1
    - grep -Ec "purchase|canAfford|credits" tests/engine/golden-path.test.ts returns at least 1
    - npx vitest run tests/engine/golden-path.test.ts exits with code 0
  </acceptance_criteria>
  <done>The golden-path file now contains GP7 (legitimate full Phase-4 path with stable hash) and GP8 (force-unlock → Modified), both passing deterministically against the real engines + store.</done>
</task>

<task type="auto">
  <name>Task 2: Requirement traceability + full-suite + typecheck gate</name>
  <files>none (verification only — findings go into the report written in Task 4)</files>
  <read_first>.planning/phases/04-post-career-and-sheet/04-DESIGN.md, .planning/ROADMAP.md, src/engine/psionics.ts, src/engine/equipment.ts, src/engine/legitimacy.ts, src/components/psionics, src/components/equipment, src/components/character-panel</read_first>
  <action>
    Build a requirement → evidence map covering all 15 Phase 4 requirements, then run
    the full-suite and typecheck gates. Capture the exact commands and outputs for the
    verification report (Task 5).

    1. For each requirement, record the satisfying test(s) and/or component(s):

       | Req | Description | Evidence (test / component) |
       |-----|-------------|-----------------------------|
       | PSIN-01 | PSI = 2D − terms served | GP7 PSI-roll assertion; src/engine/psionics.ts; tests/engine/psionics.test.ts |
       | PSIN-02 | Talent DMs (Telepathy +4 … Teleportation +0) | GP7 rolled-talent assertion; TALENT_DMS data; psionics tests |
       | PSIN-03 | Cumulative −1 per prior attempt | GP7 second-attempt DM assertion; psionics engine + tests |
       | PSIN-04 | First-chosen Telepathy auto-granted, no roll | GP7 auto-grant assertion; psionics engine + tests |
       | PSIN-05 | Acquired talents stored | GP7 talent-storage assertion; store field; psionics step UI (04-05) |
       | PSIN-06 | Talent powers (cost/reach) stored | GP7 powers assertion; src/data psionics table; psionics step UI |
       | EQUP-01 | Full Core catalog (6 categories) | tests/data equipment golden-data tests (04-02); src/data equipment catalog |
       | EQUP-02 | Purchase against mustering-out credits | GP7 purchase assertion; equipment step UI (04-05) |
       | EQUP-03 | Clamp to credits, cannot overspend | GP7 over-budget rejection; src/engine/equipment.ts; equipment tests |
       | EQUP-04 | Items carry real stats (discriminated union) | equipment golden-data tests; src/types/equipment.ts |
       | SHEE-01 | Live progressive panel during creation | CharacterPanel/Dossier (04-06); existing panel tests |
       | SHEE-02 | Final review sheet | GP7 sheet/complete; Dossier final view (04-06) |
       | SHEE-03 | Print/PDF (Field-Manual print stylesheet) | print stylesheet (04-06); human UAT print step (Task 4) |
       | SHEE-04 | Legitimacy hash on sheet | GP7 stable-hash assertion; src/engine/hash.ts; hash bar |
       | SHEE-05 | Legitimate vs Modified indicator | GP8 isModified flip; badge derivation (04-06); legitimacy engine |

       Mark any requirement lacking an automated test as **human-only** and route it to
       Task 4 (UAT) — do not silently drop it. SHEE-03 (print/PDF fidelity) and SHEE-01
       (live panel feel) are expected to be human-verified.

    2. Run the full suite. NOTE: vitest config EXCLUDES .claude worktrees — the real
       suite is ~796+ tests. Orphaned-worktree inflation (~7x) is a known artifact; if
       the count looks ~5000+, confirm the config exclude is active rather than panicking.

       Command:
         npx vitest run 2>&1 | tail -30

    3. Run the typecheck gate:
         npx tsc --noEmit 2>&1 | tail -20

    Record both outputs verbatim for the report.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -30</automated>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits with code 0 (all test files pass)
    - npx tsc --noEmit exits with code 0 (no type errors)
    - Every one of PSIN-01..06, EQUP-01..04, SHEE-01..05 has at least one recorded test or component as evidence
  </acceptance_criteria>
  <done>All 15 Phase 4 requirements are mapped to concrete evidence; the full vitest suite is green and tsc --noEmit is clean. Outputs captured for the report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author the human UAT script and walk it</name>
  <files>.planning/phases/04-post-career-and-sheet/04-HUMAN-UAT.md</files>
  <read_first>.planning/phases/02-pre-career-creation/02-UAT.md, .planning/phases/04-post-career-and-sheet/04-DESIGN.md, src/components/psionics, src/components/equipment, src/components/character-panel</read_first>
  <action>
    Create `.planning/phases/04-post-career-and-sheet/04-HUMAN-UAT.md` as a concrete
    manual test script (match the style of 02-UAT.md: numbered tests, `expected:` /
    `result:` lines, a Summary block, a Gaps section). The script must walk a human
    through the full post-career flow and explicitly check the Legitimate vs Modified
    badge at each path. Include explicit expected results per step.

    Required tests in the script (with concrete expected results):

    1. **Reach muster-out done.** Build/continue a character through mustering out.
       expected: Mustering-out summary shows credits, benefits, pension; flow offers to
       proceed to the post-career sequence. Badge reads ● Legitimate.

    2. **Psionics gate — SKIP path (not unlocked).** Character has NOT had the Unusual
       Event psionics unlock.
       expected: A psionics step that offers Skip and "Test anyway". Choosing Skip moves
       straight to Equipment and changes nothing. Badge STAYS ● Legitimate.

    3. **Psionics gate — FORCE-UNLOCK path ("Test anyway").** Restart/branch a character,
       choose "Test anyway" and confirm the explicit confirmation dialog.
       expected: A confirm prompt warns this flips legitimacy. After confirming, PSI is
       rolled (2D − terms), Telepathy auto-granted on first pick, further talents apply
       cumulative −1. Badge FLIPS to ▲ Modified and a modification entry is logged.

    4. **Psionics — legitimate unlock (if reproducible).** If you can engineer the
       Life-Events roll 12 → 1D result 1, verify reaching psionics this way keeps the
       badge ● Legitimate. (Mark N/A if not reproducible in-session — covered by GP7.)

    5. **Equipment purchase with budget enforcement.** Open the equipment step with a
       known credit balance.
       expected: Full Core catalog across 6 categories (weapons, armour, survival,
       electronics, medical, tools) with per-item stats. Buying a weapon + armour
       decrements the visible balance. Attempting to overspend is blocked (button
       disabled or rejected) and the balance never goes negative (EQUP-03).

    6. **Final sheet review (Dossier).** Reach the character sheet.
       expected: Dossier shows characteristics + DMs, skills, full career history,
       owned equipment with stats, psionic talents/powers (if any), contacts, credits,
       and the legitimacy hash. On-screen view matches the underlying data (D-1).

    7. **Print / PDF (Field Manual).** Trigger print / Save-as-PDF (Ctrl+P).
       expected: A dense single-page Field-Manual print layout renders (not the dark
       dashboard), includes the hash and the Legitimate/Modified indicator, and degrades
       gracefully if psionics/equipment are empty (SHEE-03).

    8. **Badge consistency across paths.** Confirm the badge in step 2 (skip) reads
       Legitimate and the badge in step 3 (force-unlock) reads Modified, and that the
       badge shown on the final sheet/print matches the path taken.
       expected: Skip → Legitimate everywhere; Force-unlock → Modified everywhere.

    Then WALK the script in the browser (npm run dev), recording result: pass/issue and
    severity for each test, exactly like 02-UAT.md.
  </action>
  <verify>User runs the UAT script in-browser and records pass/issue per test; confirms both psionics paths, budget enforcement, sheet, and print render with the correct badge.</verify>
  <done>04-HUMAN-UAT.md exists with 8 concrete tests and explicit expected results; the user has walked it and recorded results (type "approved" or describe issues).</done>
</task>

<task type="auto">
  <name>Task 4: Write the Phase 4 verification report</name>
  <files>.planning/phases/04-post-career-and-sheet/04-VERIFICATION.md</files>
  <read_first>.planning/phases/03-career-lifecycle/03-VERIFICATION.md, .planning/ROADMAP.md, .planning/phases/04-post-career-and-sheet/04-DESIGN.md, .planning/phases/04-post-career-and-sheet/04-HUMAN-UAT.md</read_first>
  <action>
    Create `.planning/phases/04-post-career-and-sheet/04-VERIFICATION.md` summarizing
    pass/fail against the five roadmap success criteria, matching the format of
    03-VERIFICATION.md (YAML frontmatter with verified/status/score + human_verification
    block, then Goal Achievement, Observable Truths table, Requirements Coverage table,
    Behavioral Spot-Checks, Anti-Patterns, Human Verification, Gaps Summary).

    The report MUST contain a "Success Criteria" section mapping the five Phase 4
    roadmap success criteria to pass/fail with evidence:

    | # | Success Criterion | Status | Evidence |
    |---|-------------------|--------|----------|
    | 1 | PSI test + talent learning (auto-Telepathy, rolled talents, cumulative −1) | … | GP7 + psionics tests (PSIN-01..06) |
    | 2 | Psionics event-gating + force-unlock → Modified | … | GP8 + unusual-events/legitimacy engines (D-2/D-3) |
    | 3 | Full equipment catalog + budgeted purchase (no overspend) | … | equipment golden-data + GP7 (EQUP-01..04) |
    | 4 | Live + final character sheet | … | Dossier/CharacterPanel (SHEE-01/02), human UAT |
    | 5 | PDF/print export with hash + Legitimate/Modified indicator | … | print stylesheet (SHEE-03/04/05), human UAT, GP7/GP8 |

    Include:
    - The Requirements Coverage table for PSIN-01..06, EQUP-01..04, SHEE-01..05 (reuse
      the map from Task 2) with SATISFIED / PARTIAL / human-only status + evidence.
    - Behavioral Spot-Checks table with the exact commands and results:
        npx vitest run tests/engine/golden-path.test.ts  → pass
        npx vitest run                                     → N/N pass (note the worktree-exclude caveat)
        npx tsc --noEmit                                   → no errors
    - A human_verification frontmatter block pointing at 04-HUMAN-UAT.md (the blocking
      in-browser walkthrough: skip path, force-unlock path, budget enforcement, sheet,
      print) and any items that are human-only by nature (print fidelity, live-panel feel).
    - A Gaps Summary. If all five criteria pass and the suite is green, state that
      explicitly; otherwise list each gap with severity and the artifact(s) involved.

    Set frontmatter `status: human_needed` if Task 3 has unresolved issues, otherwise
    `verified`. Set `score: M/N must-haves verified`.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-post-career-and-sheet/04-VERIFICATION.md && grep -c "Success Criteria" .planning/phases/04-post-career-and-sheet/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/04-post-career-and-sheet/04-VERIFICATION.md exists
    - grep -c "Success Criteria" returns at least 1
    - grep -Ec "PSIN-01|EQUP-01|SHEE-01" returns at least 1
    - All five roadmap success criteria appear with a pass/fail status and evidence
  </acceptance_criteria>
  <done>04-VERIFICATION.md summarizes pass/fail against the five Phase 4 success criteria, includes the full requirements-coverage map and behavioral spot-checks, and references the human UAT for the blocking in-browser items.</done>
</task>

</tasks>

<verification>
- npx vitest run tests/engine/golden-path.test.ts --reporter=verbose — GP7 + GP8 pass deterministically
- npx vitest run — full suite green (worktree-exclude caveat noted; ~796+ real tests)
- npx tsc --noEmit — no TypeScript errors
- 04-HUMAN-UAT.md walked in-browser; both psionics paths, budget enforcement, sheet, and print verified with correct badge
- 04-VERIFICATION.md maps all 15 requirements + five success criteria to evidence
</verification>

<success_criteria>
- A Phase-4 golden character (GP7) runs end-to-end through the pure engines + store:
  legitimate psionics unlock, PSI roll, Telepathy auto-grant + one rolled talent,
  weapon + armour purchase against credits, sheet/complete with a stable legitimacy hash
- A second golden character (GP8) force-unlocks psionics and flips isModified → true with a logged entry
- Every Phase 4 requirement (PSIN-01..06, EQUP-01..04, SHEE-01..05) maps to a test/component
- Full vitest suite green and tsc --noEmit clean
- 04-HUMAN-UAT.md provides a concrete manual script (skip path, force-unlock path,
  budgeted purchase, sheet review, print/PDF) with explicit expected results and badge checks
- 04-VERIFICATION.md reports pass/fail against the five roadmap success criteria
</success_criteria>

<output>After completion, create .planning/phases/04-post-career-and-sheet/04-07-SUMMARY.md</output>
