---
phase: 02-pre-career-creation
task: null
total_tasks: null
status: blocked
last_updated: 2026-04-02T13:41:29.591Z
---

<current_state>
Phase 02 has all 10 plans executed (5 original + 5 gap closure). UAT retest round 2 found a BLOCKER: white screen crash when transitioning from background skills confirmation to the education step. 3 tests blocked by this crash. 2 minor issues also found (drag snap-back, tooltip line break).

The debug agent was spawned to diagnose the white screen but hit usage limits before completing.
</current_state>

<completed_work>

- Plans 02-01 to 02-05: Original Phase 2 implementation (wizard shell, data layer, characteristics, background skills, education)
- Plans 02-06 to 02-08: Gap closure round 1 (drag offset, session persistence, confirmation dialogs, education UX, roll log labels, relevance tooltips)
- Plans 02-09 to 02-10: Gap closure round 2 (background skills drag overlay, tooltip overlap, duplicate skills, entry odds, GO_BACK button)
- UAT round 1: 9 passed, 6 issues → all diagnosed and fixed
- UAT retest 1: 4 passed, 4 issues → all diagnosed and fixed
- UAT retest 2: 0 passed, 3 issues, 3 blocked → WHITE SCREEN BLOCKER
</completed_work>

<remaining_work>

1. **BLOCKER: White screen after background skill confirmation** — Diagnose and fix. Likely XState transition from backgroundSkills.review CONFIRM → education is broken. Check creation.ts machine, WizardShell rendering, EducationStep mount.
2. **Minor: Drag snap-back** — Background skill card briefly zips back to pool after drop (BackgroundSkillsStep.tsx DragOverlay/onDragEnd)
3. **Cosmetic: Tooltip line break** — Combined tooltip text too long, relevance info needs separate line (SkillPool.tsx)
4. **Blocked tests** — Once blocker fixed, retest: duplicate skills, already-owned indicator, entry odds on cards, back button on pre-roll
</remaining_work>

<decisions_made>

- Zustand persist with sessionStorage for character data (session-scoped, not persistent)
- Event replay pattern for XState machine fast-forward on reload
- Confirmation dialog pattern: Card with amber border-l and warning text
- Combined single tooltip instead of nested tooltips (avoids group-hover conflict)
- addSkill made idempotent: skip if exists at same/higher level, upgrade if lower
</decisions_made>

<blockers>
- White screen crash after backgroundSkills.review → education transition. All 1,139 unit tests pass, so this is a runtime/rendering issue not caught by tests. Debug agent was spawned but ran out of usage before completing.
</blockers>

<context>
The white screen is almost certainly caused by the interaction between plan 02-07 (added nested backgroundSkills selecting/review sub-states with CONFIRM event) and plan 02-10 (added GO_BACK event to education sub-states). The CONFIRM transition from backgroundSkills.review likely targets the wrong state or the education sub-state initialization is broken. Check:
1. creation.ts: backgroundSkills.review.on.CONFIRM — where does it transition to?
2. WizardShell.tsx: does it map the new machine state to the education step component?
3. EducationStep.tsx: does it crash on mount with undefined props?
4. useCreationMachine.ts: does deriveReplayEvents handle SKILLS_SELECTED + CONFIRM correctly?
</context>

<next_action>
Start with: Diagnose the white screen blocker by tracing the XState machine transition path from backgroundSkills.review CONFIRM through to education state rendering. Check browser console for errors. Fix the transition, then retest all blocked UAT items.
</next_action>
